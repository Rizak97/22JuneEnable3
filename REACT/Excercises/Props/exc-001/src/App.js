import "./App.css";
import Players from "./players.jsx"

function App() {
  return (
    <div>
      <h5 className="task1" height="20px;">
        In this Exercise, you should edit your App.js page, using the general
        layout from the example in the module, to render some HTML on the page
        with some CSS styling
      </h5>
      {/* Inside the React application you made in the Introduction module,
      implement some JSX which renders a component on the screen */}
      
      <Players />
    </div>
  );
}

export default App;
